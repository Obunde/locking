<h1>Received Orders</h1>

<table border=1>
  <thead>
    <tr>
      <td>Item Name</td><td>Quantity</td>
    </tr>
  </thead>
  <tbody>
  <% @items.each do |item| %>
    <tr>
      <td><%= item.name %></td>
      <td><%= item.total_quantity %></td>
    </tr>
  <% end %>
  </tbody>
</table>
